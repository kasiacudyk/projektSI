{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_task_details'|trans({'%id%': todolist.id|default('')}) }}
{% endblock %}

{% block body %}
    {% block header %}
        <h1>{{ 'virtual_note'|trans }}</h1>
    {% endblock %}

    <h2>{{ 'title_task_details'|trans({'%id%': todolist.id|default('')}) }}</h2>
    {% if todolist is defined and todolist|length %}
        <dl>
            <dt>{{ 'label_id'|trans }}</dt>
            <dd>{{ todolist.id }}</dd>
            <dt>{{ 'label_title'|trans }}</dt>
            <dd>{{  todolist.title }}</dd>
        </dl>
        <p>
            <a href="{{ url('todolist_index') }}" title="back to the list">
                {{ 'button_back'|trans }}
            </a>
        </p>
    {% else %}
        <p>
            {{ 'message_item_not_found'|trans }}
        </p>
    {% endif %}
{% endblock %}