{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_notes_edit'|trans({'%id%': notes.id|default('')}) }}
{% endblock %}

{% block body %}
    {% block header %}
        <h1>{{ 'virtual_note'|trans }}</h1>
    {% endblock %}

    {% include 'default/_form.html.twig' with {
        page_title: 'title_notes_edit',
        page_title_param: {'%id%': notes.id},
        attr: {
            method: 'PUT',
            action: url('notes_edit', {'id': notes.id}),
        },
        form: form,
        back_to_list_path: 'notes_index',
    } %}
{% endblock %}